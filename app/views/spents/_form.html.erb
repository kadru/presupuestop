<%= form_with(model: spent) do |form| %>
  <% if spent.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(spent.errors.count, "error") %> prohibited this spent from being saved:</h2>

      <ul>
        <% spent.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
   <%= form.label :name %>
   <%= form.text_field :name %>

   <%= form.label :amount %>
   <%= form.number_field :amount %>

   <%= form.label :category_id %>
   <%= form.select :category_id, categories %>

   <%= form.label :subcategory_id %>
   <%= form.select :subcategory_id, subcategories %>
   <%= form.submit %>
  </div>
<% end %>
